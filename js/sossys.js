/**
 * Sassy Saloon is now a first-class FLY_LANDMARK (kind: sassySaloon)
 * with Kurtis Not Hall on piano — see world.js + game.js.
 * This stub keeps the old script tag from erroring.
 */
(function () {
  /* no-op — landmark visit system owns the saloon */
})();
